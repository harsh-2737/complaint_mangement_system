@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model complaint_mangement_system.ViewModels.StaffRequestViewModel

@{
    ViewData["Title"] = "Approve Staff";
}

<div class="container mt-4">

    <div class="card shadow-sm">

        <div class="card-body text-center p-5">

            <i class="bi bi-check-circle-fill text-success fs-1"></i>

            <h2 class="mt-3">
                Approve Staff Request
            </h2>

            <p class="text-muted mt-3">
                Are you sure you want to approve this staff request?
            </p>

            <div class="mt-4">

                <p>
                    <strong>Staff ID:</strong>
                    @Model.staffid
                </p>

                <p>
                    <strong>Name:</strong>
                    @Model.name
                </p>

                <p>
                    <strong>Category:</strong>
                    @Model.categoryname
                </p>

            </div>

            <form asp-controller="Admin"
                  asp-action="ConfirmApproveStaff"
                  method="post">

                <input type="hidden"
                       name="staffid"
                       value="@Model.staffid" />

                <button type="submit"
                        class="btn btn-success me-2">
                    <i class="bi bi-check-circle me-1"></i>
                    Yes, Approve
                </button>

                <a asp-controller="Admin"
                   asp-action="StaffRequests"
                   class="btn btn-secondary">
                    Cancel
                </a>

            </form>

        </div>

    </div>

</div>
